# c016

- 標準メールの確認リンクから、公式SDKでセッションを検証・保存してログインする方式へ変更。
- コード入力欄を廃止し「ログイン用メールを送る」に変更。
- 認証情報はURL履歴から速やかに除去。無効リンクでは新規セッションを作らない。
- リンク処理後は人物・設定へ移動。初回引き継ぎ前に端末内の人物数・会話記録数を表示。
- iPadのデータを基準に、iPhoneはクラウド側を選択する運用。元の端末データは保持。

# c015

- 過去記録が見えない事象の調査用に「保存データを確認」を追加。
- ログイン前と現在の保存先の件数を読み取り専用で表示。人物名・メール・ID・トークンは報告に含めない。
- 空のアカウントDBに切り替えると旧記録が表示されなくなる状態を再現。原本保持・人数を含む引き継ぎを検証。
- ユーザー実機の原因は診断結果待ち。自動復元・データ再送・認証方式の変更は行わない。

# c014

- 指定されたSupabaseプロジェクトのURL・公開キーをローカル環境設定へ追加。
- Auth設定APIへの接続とメール認証有効を確認。
- people / daily_logs / settings / profilesはRESTから未検出。DashboardでSQL適用が必要。
- 秘密鍵・環境設定ファイルはGitに含めない。

# c013

- 公式Supabaseクライアントを同梱。メールOTPログインと任意の同期設定を追加。
- アカウント別IndexedDB、保存と一体の永続pending、双方向同期、再試行、削除情報の伝播。
- 旧端末データを残した初回の引き継ぎ選択。ログアウト時は旧端末DBへ戻る。
- 全クラウドテーブルのRLS・所有者の複合外部キー・原子的LWW関数を追加。
- ローカルPostgreSQLのA/B隔離テスト、実環境REST検証スクリプト、設定手順を追加。
- CSP、公開キーのみのビルド検証、開発サーバーのファイル公開範囲制限。
- Supabase URL・公開キー未設定では端末内保存のみ。実プロジェクト設定・実機同期・実REST確認は別途必要。

# 012

- カレンダーは茎・葉を省き、花を日付枠に合わせて拡大。
- 紫陽花の小花を丸く密に配置。入力画面にも共通反映。
- 日付枠の高さ・既存記録・0人の双葉は維持。

# 011

- 12か月の花名に対応する異なる植物SVGへ変更。
- 人数を共通開花進捗に変換し、5人で完成・6人以上は配色のみ変化。
- カレンダーと入力で同じ描画を共用。記録データは維持。

# 更新履歴

## 010 — 2026-09-11

- 「…」に「一覧から外す」を追加しました。isActive=falseだけを更新し、入力・設定の通常カードから除外します。
- 過去ログと花の合計人数はそのまま保持します。設定奥の折りたたみから「一覧に戻す」ができます。完全削除は追加していません。

## 009 — 2026-09-11

- 現在の3列カードを維持し、1人の相手と複数人のグループに対応しました。淡いクリーム・緑で区別します。
- 登録済みカードに「…」を追加し、種類・名前・非表示を設定できます。自由入力は追加時だけ種類を選択します。
- グループはタップで1人、選択後だけ＋－が表示されます。カードの再タップは人数に関係なく全解除です。合計人数を入力画面・カレンダーの共通描画へ反映します。
- IndexedDBをversion 2へ移行し、旧データのID・日付・並び順・非表示・人数を保持します。新規利用時のみ11枚の基本カードを用意します。
- 過去の複数人記録は種類を1人へ変えても減らさず、その記録には人数調整を残します。新しい記録は現在の種類で作成します。

## 008 — 2026-09-11

- 設定を開いた直後の人物一覧を3列のボタン画面に変更しました。同じ画面で候補から追加・名前入力・編集・並び替えができます。
- ドラッグ並び替えを3列グリッドに対応させ、矢印キーでの並び替えも追加しました。

## 007 — 2026-09-11

- 初回登録と追加登録を同じ候補3列＋名前入力の画面に統一し、初回も追加ごとに保存します。
- 会話入力の人物ボタンを3列に変更しました。
- 設定の人物一覧にタッチ・マウス対応のドラッグハンドルを追加しました。上下ボタンも維持し、順序は入力画面にも反映します。

## 006 — 2026-09-11

- 過去日・今日の入力を許可し、未来日のタップは理由の説明を表示して入力へ進まないようにしました。直接URL・保存処理でも未来日を拒否します。
- 開始日前でも記録が1人以上あれば人数に応じた花を表示します。0人は開始日前なら空欄、開始日以降なら双葉です。
- 旧版の未来日の記録は削除せず保持し、その日以降に編集できます。

## 005 — 2026-09-11

- 10/1の妻の記録を保存・再読込し、金木犀の花びら1枚を表示する回帰テストを追加しました。
- 古いキャッシュが残り続けないよう、全資産を再取得した後に新しいService Workerを有効化します。入力途中で画面を強制再読込しません。
- 設定に「最新版を読み込む」を追加しました。記録を削除せず更新できます。

## 004 — 2026-09-11

- 設定からの人物追加を候補ボタンと名前のみの入力画面に変更しました。候補はタップで即時保存し、続けて追加できます。
- 候補のカテゴリは自動設定、自由入力はその他として追加します。詳細編集は既存の編集ボタンから利用できます。
- 登録済みの候補は重複追加せず、非表示の人物は同じID・履歴のまま再表示できます。

## 003 — 2026-09-11

- カレンダーは日付の条件より会話記録を優先し、1人以上なら必ず人数に応じた花を表示します。
- 双葉は利用開始日以降の0人の日だけに表示します。利用開始日前でも記録があれば花を表示します。

## 002 — 2026-09-11

- カレンダートップのキャッチコピー・説明領域を省き、花のサイズを保って余白を圧縮しました。説明は設定画面に移動しました。
- 月別の季節の一言をseasons.jsに追加しました。
- 初回起動でappStartedAtを一度だけ保存します。既存データは登録・記録の最古の日付から補完し、以後は上書きしません。
- 利用開始日前は空欄、開始日以降の0人の日と未来日は控えめなSVG双葉にしました。入力画面の花は維持しています。

## 001 — 2026-09-11

- 花を参考画像への変更前のデザインに戻しました（茎・葉・中央の丸がある花）。
- 効果音と中立的なコメントは維持しています。
- 設定画面にバージョン番号を表示します。
- この版を連番管理の起点とし、次回は002、その次は003です。

過去の開発履歴はGitに保持しています。Sitesの自動採番とは別のアプリバージョンです。

c016追加: ホーム画面版の初回画面に既存クラウドデータの入口を追加。未使用の確認メールリンクを貼り付け、開いているPWA内でログインを完了する方式を追加。既存ブラウザのIndexedDBを保持。認証リンク・保存・同期・RLSなど35件の自動テストを確認。

## c017
メール送信失敗を、送信上限・宛先制限・認証設定・通信障害に分けて表示します。個人情報を含み得るサーバーの生メッセージは表示・保存せず、確認番号のみ表示します。送信失敗時にも入力アドレスを保持します。実機の失敗原因は確認番号の取得待ちです。

## c018
Googleログインボタン、未設定時の案内、アカウント選択を追加。メールログインは折りたたみへ移動。Google/Supabase設定手順をREADMEへ追加。既存の端末保存と移行確認を保持。Dashboard設定と実機認証は利用者の設定待ち。

## c019
会話入力画面を日付・花・人数・人物選択の順へ変更。重複見出しと人物カードの＋を削除。3列カードを高さ74pxで固定し、選択済みグループのみ同じ枠内に人数調整を表示。画面移動時は先頭表示、操作中のスクロール位置は保持。追加欄の入力例を鈴木さんへ変更（実データの登録なし）。月別SVG・保存・同期・カウント・効果音の処理は維持。

確認: 自動テスト37件、ローカルChromeの375×600px表示で花・カード9件が表示領域内。個人の選択解除、グループ増減/0解除、40%花表示、再読込後の保持、過去日入力、未来日拒否、設定の入力例を確認。グループ選択前後で高さとスクロール位置に変化なし。実iPhone Safariでの検証は未実施。

## c020
初回画面を「はじめて使う方」「すでに利用している方」の同等な案内へ変更。既存利用のボタンからGoogleログイン後、人物・会話・設定の取得、アカウント別ローカル反映、同期有効化、カレンダー表示まで自動化。認証/取得中は読み込み画面、空クラウドは人物登録、失敗は再試行を表示。端末の未同期データがある場合は同一トランザクションで検出して停止。通常のログイン・手動移行は維持。

## c021
ホーム画面への追加名をhanatalkへ変更。manifestのname/short_nameとiOS向けapple-mobile-web-app-titleを統一。アプリ内タイトル・PWAのid/start_url/scope・保存先は変更なし。

## c022
初回の人物登録にも設定画面と同じ編集可能な3列カードを使用。「…」から名前・種類変更、一覧から外す、ドラッグ並び替え、非表示カードの復帰を提供。保存・同期・会話履歴の処理は変更なし。

## c023
定期同期で変更のない画面を再描画しないよう修正。リモート反映は表示データの変更有無を返し、変更があった場合のみ更新。入力・編集中は表示更新を保留。同期状態はテキストのみ更新し、同一アカウントの認証更新時も設定パネルを作り直さない。同期頻度・データ形式・保存先は維持。

## c024
ユーザー向け正式名をHanaTalkへ統一し、人物追加の入力例を「鈴木さん、カラオケ仲間 など」へ変更。GitHub Pages用Actions、公開接続設定、/hanatalk/確認サーバーを追加。認証リダイレクトをアプリの実行ディレクトリに合わせ、PWAキャッシュをscopeで分離。データ形式とIndexedDB名・Supabase/RLSは維持。GitHub公開とDashboard設定は別途必要。
# c025

- 初回設定未完了時に、芽・HanaTalk・短い紹介・「はじめる」のタイトル画面を表示。
- ホーム画面追加の案内を追加し、standalone起動時には非表示。
- initializedフラグと既存人物登録を維持。「はじめる」では初回設定を完了にしない。
- 記録引き継ぎの入口は人物登録画面の折りたたみに移動。
